## 1. Calendar filter state

- [x] 1.1 In `CalendarPage.vue`, replace `dayListChannelFilter: ref<string>('all')` with `dayListSelectedChannels: ref<Set<string>>(new Set())` — a positive whitelist of explicitly selected channel keys, empty by default (design.md Decisions 1-2) — and add an `enabledChannelKeys` computed that resolves it to "every current channel" when empty, or the selection itself otherwise (design.md Decision 3) — verify with `pnpm --filter mobile run build` (type-checks cleanly, no remaining references to the old field)
- [x] 1.2 Add `toggleDayListChannel(key: string)` (flips membership in `dayListSelectedChannels`) and `enableAllDayListChannels()` (clears the set, i.e. activates "All") functions, mirroring the existing `toggleDayListStatus`/`toggleDayListDiscountType` pattern — verify by unit-testing or manually toggling in the dev server that the set updates as expected

## 2. Grid line filtering

- [x] 2.1 In `utils/calendar.ts`, add an `enabledChannelKeys: Set<string>` parameter to `getDayChannelLines` and filter `weekChannelOrder` to only channels present in it (design.md Decision 3) — verify with a fixture test (or manual check): a channel excluded from `enabledChannelKeys` produces no entry in any day's returned slot array for a week it would otherwise appear in, and other enabled channels in that week are not left with a blank gap where it used to be
- [x] 2.2 Update `CalendarPage.vue`'s `dayChannelLines` computed to pass `enabledChannelKeys.value` into `getDayChannelLines` — verify by disabling a channel in the dev server (jeep-sqlite/web preview) and confirming its lines disappear from every day in the currently visible month, with adjacent channels' lines shifting to fill the freed slot

## 3. Day-list filtering

- [x] 3.1 In `utils/calendar.ts`, change `DayListFilters.channelFilter` from `string` to `Set<string>` (the enabled set) and update `filterDayListCoupons`'s channel check accordingly (design.md Decision 4): a coupon is excluded only if it has a channel and that channel's key is absent from the set; a coupon with no channel is never excluded by this filter — verify with a fixture test covering: an enabled channel's coupon is kept, a disabled channel's coupon is excluded, and a coupon with no channel (deleted channel) is always kept regardless of the set's contents
- [x] 3.2 Update `CalendarPage.vue`'s `selectedDayCoupons` computed to pass `enabledChannelKeys.value` as `filterDayListCoupons`'s `channelFilter` — verify by selecting a specific channel in the dev server and confirming the selected-day list below shows only that channel's coupons, while status/discount-type filters and sort continue to work unchanged

## 4. Toolbar and legend replacement

- [x] 4.1 Add a `showChannelFilter?: boolean` prop (default `true`) to `CouponFilterToolbar.vue`, wrap its existing single-select channel-chip row in `v-if="showChannelFilter"` (design.md Decision 5) — verify `CodesPage`'s usage (no prop passed) still renders the channel row exactly as before
- [x] 4.2 In `CalendarPage.vue`, pass `:show-channel-filter="false"` on its `<CouponFilterToolbar>` usage below the "Active on {date}" heading, and stop passing/reading a channel-filter value there — verify that toolbar now shows only sort/status/discount-type controls, no channel chips
- [x] 4.3 Remove `.cal-legend` and its markup/styles from `CalendarPage.vue`; add a new toggle-chip row in its place (below `.cal-grid`, inside `.cal-header`) rendering one chip per `channelsStore.items` entry (channel color + name) plus an "All" chip, styled like `CouponFilterToolbar`'s multi-select `filter-chip`/`filter-chip.on` classes (design.md Decision 5) — verify visually in the dev server: the chip row sits directly under the grid where the legend used to be, each chip shows the channel's color and name, and a selected channel's chip is visually distinguished from an unselected one
- [x] 4.4 Bind each chip's `on` state directly to `dayListSelectedChannels` ("All" is `on` iff the set is empty; a channel chip is `on` iff its key is in the set) and wire clicks to `toggleDayListChannel`/`enableAllDayListChannels` from task 1.2 (design.md Decision 6: this single derivation is what makes "All" and the per-channel chips behave as one mutually-exclusive-looking filter with no separate mode flag) — verify: on first load "All" is the only chip shown as selected and every channel's lines/coupons are visible; selecting a channel deselects "All" and narrows the grid/list to just that channel; selecting an additional channel adds to what's shown without removing the first; deselecting the only selected channel reverts to "All" and shows everything again; activating "All" directly clears any selection and deselects every channel chip

## 5. End-to-end verification

- [x] 5.1 Run `pnpm --filter mobile run build` and confirm it passes (type-check + production build) with no leftover references to the old `dayListChannelFilter`/`DayListFilters.channelFilter` string shape
- [x] 5.2 In the dev server (`pnpm --filter mobile run dev`), open Calendar on a month with coupons across at least two channels: confirm "All" is selected by default with every channel's lines/coupons visible; selecting one channel deselects "All" and narrows the grid/list to just it; selecting a second channel adds to (not replaces) what's shown; deselecting the last selected channel reverts to "All"; activating "All" directly clears any selection; and the Codes list's own single-select channel filter is visually and behaviorally unaffected throughout
- [x] 5.3 Confirm the status filter, discount-type filter, and sort control below the "Active on {date}" heading still work exactly as before (combining correctly with the new channel visibility filter), and that the shared default-sort behavior (Codes list / Settings screen / Calendar day list) is unchanged

## 6. Second revision: collapse-to-"All" + Codes parity

- [x] 6.1 Add `utils/channelSelection.ts` with `toggleChannelSelection(current, key, totalChannelCount)` (toggles membership, then collapses to an empty set once every channel is selected — design.md Decision 7) and `resolveSelectedChannelKeys(selected, allKeys)` (empty ⇒ every key) — verify with `pnpm --filter mobile run build`
- [x] 6.2 Update `CalendarPage.vue`'s `toggleDayListChannel` to call `toggleChannelSelection` instead of its inline add/remove logic, and its `enabledChannelKeys` computed to call `resolveSelectedChannelKeys` — verify in the dev server: selecting every channel one at a time ends with "All" shown as active and every channel chip shown as not active, same grid/list as before the last click
- [x] 6.3 Add `components/ChannelFilterChips.vue` (props `channels: ChannelRow[]`, `selected: Set<string>`; emits `toggle: [string]`, `select-all: []`) rendering the "All" + per-channel chips with the same markup/CSS `CalendarPage.vue`'s inline row used (design.md Decision 8) — verify visually: identical to the previous inline row
- [x] 6.4 Replace `CalendarPage.vue`'s inline chip markup with `<ChannelFilterChips>`, removing the now-duplicate CSS from `CalendarPage.vue` — verify the Calendar screen's channel filter still looks and behaves exactly as in task group 4/5
- [x] 6.5 In `db/queries/coupons.ts`, change `CouponListFilter.channelKey?: string` to `channelKeys?: string[]` and the channel-matching join's `where(eq(channels.key, filter.channelKey))` to `where(inArray(channels.key, filter.channelKeys))`, gated on `filter?.channelKeys?.length` (design.md Decision 8) — verify with `pnpm --filter mobile run build` and a manual check that filtering by one channel still returns the same rows as before
- [x] 6.6 In `stores/coupons.ts`, change `channelFilter` from `'all' | string` to `Set<string>` (default empty), change `setChannelFilter` to take the already-resolved next `Set<string>`, and update `load()` to pass `channelKeys: this.channelFilter.size === 0 ? undefined : [...this.channelFilter]` — verify with `pnpm --filter mobile run build`
- [x] 6.7 In `CodesPage.vue`, add `toggleChannelFilter(key)` (calls `couponsStore.setChannelFilter(toggleChannelSelection(couponsStore.channelFilter, key, channelsStore.items.length))`) and `selectAllChannels()` (calls `couponsStore.setChannelFilter(new Set())`), and wire them to the toolbar in place of the old `@update:channel-filter` handler
- [x] 6.8 In `CouponFilterToolbar.vue`, change its `channelFilter` prop to `Set<string>` (default empty set), replace its inline single-select chip row with `<ChannelFilterChips>`, and replace the `update:channelFilter` emit with `toggle-channel`/`select-all-channels` — verify Codes' channel row now behaves as multi-select with the same "All" default/collapse behavior as Calendar's
- [x] 6.9 End-to-end verification in the dev server: on Codes, confirm "All" is selected by default, selecting a channel deselects "All" and narrows the list to it, selecting a second channel adds to what's shown, deselecting the last selected channel reverts to "All", selecting every channel individually collapses to "All", and the list stays correctly sorted/status/discount-filtered throughout; repeat the same checks on Calendar to confirm both screens behave identically; run `pnpm --filter mobile run build` one final time
