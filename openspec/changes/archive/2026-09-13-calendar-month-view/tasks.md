## 1. Month-grid and day-overlap utility

- [x] 1.1 Add `utils/calendar.ts` with a function that takes a `{ year, month }` and returns an ordered array of day cells (ISO `YYYY-MM-DD` date, `dim` for leading/trailing days from adjacent months), correctly handling weekday alignment and month length (including February in a leap year). Verify with a small standalone check (e.g. a temporary dev-console call or a quick script) for at least one 28-day, one 30-day, one 31-day, and one leap-February month, confirming cell counts and the first/last real day's weekday.
- [x] 1.2 Add a function that takes that day-cell list plus the fetched coupons and channels, and returns per-day an ordered array of distinct channel colors to render as indicator lines (a coupon's date range includes a day via ISO-string comparison on `startDate <= day <= endDate`; a coupon contributes its first channel only, per design.md Decision 3; the array is ordered by channel `sortOrder`, per design.md Decision 3a, and simply omits any channel with no active coupon that day). Verify against a small fixture (two coupons on different channels overlapping one day, one coupon with no channel, one channel present on one day but not an adjacent day) that the returned arrays match expectations, including order and omission.
- [x] 1.3 Add a function that filters the fetched coupons to those whose date range includes a given selected date, and returns them already sorted (or ready to be sorted) by days-left descending. Verify against a fixture mixing active, soon-expiring, and expired coupons that the selected day's subset and order match `specs/calendar-view/spec.md`'s "Default list order" scenario.

## 2. Month/year picker popup

- [x] 2.1 Build `components/MonthYearPickerModal.vue` using the Teleport-to-body/backdrop/bottom-sheet pattern (header with title + close, scrollable body: a year selector plus a 12-month grid for the selected year, no footer — per design.md Decision 4). Verify it opens/closes and visually matches the other four overlays (`ConfirmDialog`, `CouponDetailModal`, `CouponFormModal`, `ChannelManagerModal`).
- [x] 2.2 Wire month selection: tapping a month in the grid emits the chosen `{ year, month }` and closes the popup; dismissing via backdrop/close button emits nothing. Verify both paths in the dev preview — selecting a month closes the popup with the right value, dismissing leaves the caller's state untouched.

## 3. CalendarPage rewrite

- [x] 3.1 Replace the hardcoded `days`/`monthLabel`/`selectedDateLabel` with a `visibleMonth` ref (defaulting to the current real month) driving the grid via the section 1 utility, and a computed label formatted from it. Verify the grid renders the actual current month with correct weekday alignment on first load.
- [x] 3.2 Wire the `‹`/`›` header controls to decrement/increment `visibleMonth` (handling year rollover at January/December). Verify repeatedly clicking each control steps through months and years correctly, including across a year boundary.
- [x] 3.3 Wire the month/year label to open `MonthYearPickerModal`; on its selection event, set `visibleMonth` to the chosen value. Verify picking a month/year several years away updates the grid and label to match, per `specs/calendar-view/spec.md`'s "Jumping to a specific month and year" scenario.
- [x] 3.4 Replace the hardcoded `selected`/`today` flags with a `selectedDate` ref (defaulting to the device's actual current date) and a `today` computed from the real current date; make every day cell (including dimmed leading/trailing days) clickable, setting `selectedDate` and, for a dimmed day, also updating `visibleMonth` to that day's month. Verify selecting an in-month day updates the list below without changing `visibleMonth`; selecting a dimmed day changes `visibleMonth` and re-renders the grid with that day selected and no longer dimmed.
- [x] 3.5 Change `.cal-days .day.sel`'s border color from `var(--ck-sage)` to `var(--ck-ink)` (design.md Decision 6); confirm the existing `.day.today` filled style is unchanged and still applies (not overridden by `.sel`) when a day is both today and selected. Verify visually against the reference image's black-ringed selected day.
- [x] 3.6 Replace `mockPromoCodes` with real data: load `useChannelsStore()` and call `listCoupons({ sortBy: 'daysLeft', sortDir: 'desc' })` once when the page is entered (e.g. `onIonViewWillEnter`, matching `CodesPage`'s pattern), store the results in page-local refs (not `useCouponsStore`, per design.md Decision 1). Verify the fetch runs once per visit and doesn't touch `useCouponsStore`'s state (Codes tab's own filter/sort selections are unaffected after visiting Calendar).
- [x] 3.7 Wire the day indicator lines (section 1.2 utility) and the legend to the loaded channels (replacing the hardcoded 3-channel legend), and the selected-day list (section 1.3 utility) to the loaded coupons. Replace `.marks`/`.m`'s dot styling with thin (~2px) horizontal lines, inset a small amount from the top of `.day` and stacked with a small vertical gap between them, per design.md Decision 3a. Verify a day with two different-channel coupons shows two correctly colored, correctly ordered lines with no gap between them, a day with none shows no lines, a day where one of two channels has no active coupon shows only the other channel's line (no blank gap in the missing channel's place), and the legend lists every real channel (including a newly-added custom one).
- [x] 3.8 Render the selected-day list with `CodeCard` (no `deletable` prop) and open `CouponDetailModal` on `@select`, mirroring `CodesPage`'s wiring minus delete. Verify tapping a card opens the detail popup with correct data and no delete control appears anywhere in the Calendar flow.

## 4. Localization

- [x] 4.1 Add i18n strings for `MonthYearPickerModal` (title, any close/label text) to `en.ts`; confirm existing `calendar.*` strings (title, weekdays, legend, activeOn) still cover the rewritten page, adding any missing ones. Verify by grepping the new/changed components for literal user-facing strings that bypass `t()`.

## 5. Verification

- [x] 5.1 Run `pnpm --filter mobile run build` and confirm it passes with zero type errors.
- [x] 5.2 In the web dev preview (`pnpm --filter mobile run dev`), exercise every scenario in `specs/calendar-view/spec.md`: month navigation via arrows, the month/year picker (both selecting and dismissing), selecting in-month and adjacent-month days, the default-today selection on first load, the channel indicator lines and legend against real seeded/created channels and coupons, the selected-day list's contents/order/empty case, and opening a coupon's detail popup with no delete control.
- [x] 5.3 Per CLAUDE.md's testing guidance, run `npx cap sync android` and repeat the same flow on the `Vichara_API36` emulator (not just the web/jeep-sqlite fallback), since this change adds a new unfiltered full-table `listCoupons`/`listChannels` read path exercised from a second screen. Confirm behavior matches the web preview.

## 6. Follow-up: indicator-line polish and today color

- [x] 6.1 In `CalendarPage.vue`, increase `.chan-lines`'s stacking `gap` (2px → 4px) and remove the `.chan-line`/`.chan-lines` horizontal inset (`left`/`right`) so lines span the full cell width edge-to-edge; verify visually that same-colored lines in horizontally adjacent day cells appear to fuse into one continuous bar (`.cal-days` already has no `column-gap`, so zero inset is sufficient — no other layout change needed).
- [x] 6.2 Change `.cal-days .day` from vertically centered to bottom-anchored content (`align-items: flex-end` plus a small bottom padding) so the day number sits near the bottom edge of the cell, clear of the line stack above it. Verify the number stays legible and centered horizontally, and doesn't collide with the lines even on a day with several stacked lines.
- [x] 6.3 Change `.cal-days .day.today`'s background from `var(--ck-ink)` to `var(--ck-sage)` and its text color to `#fff` (design.md Decision 6a); remove the `.cal-days .day.today.sel { border: none }` override so the selected black ring renders normally even when today is also selected. Verify today's cell reads as a sage-filled square, and that selecting today (or navigating back to it after selecting elsewhere) shows both the sage fill and the black ring together.

## 7. Follow-up: shared filter/sort toolbar

- [x] 7.1 Extract `CodesPage.vue`'s channel-chip row and sort/status/discount-type toolbar into `components/CouponFilterToolbar.vue` (props: `channels`, `channelFilter`, `statusFilter`, `discountTypeFilter`, `sortBy`, `sortDir`; emits for each control), per design.md Decision 7 — a pure relocation, no behavior change. Update `CodesPage.vue` to render it bound to `useCouponsStore`'s existing state, keeping its own "manage channels" `+` button outside the extracted component. Verify Codes' filter chips, sort dropdown, and status/discount chips still look and behave exactly as before (same scenarios as `specs/coupon-list-filtering/spec.md`).
- [x] 7.2 Add local filter/sort state to `CalendarPage.vue` (`dayListChannelFilter`, `dayListStatusFilter`, `dayListDiscountTypeFilter`, `dayListSortBy`, `dayListSortDir`, defaulting to no filter / `daysLeft` desc) and render `CouponFilterToolbar` below the "Active on [date]" heading, bound to this new local state — not `useCouponsStore`. Verify changing any control updates local state without touching `useCouponsStore` (Codes' own toolbar selections stay untouched after visiting Calendar and back).
- [x] 7.3 Add an in-memory filter+sort helper to `utils/calendar.ts` (channel/status/discount-type filtering matching `coupon-list-filtering`'s combination rule, plus `daysLeft`/`expiringSoon` comparators mirroring `queries/coupons.ts`'s `sortRowsInMemory` logic over the `PromoCode` shape) and apply it to `getCouponsForDate`'s result before rendering the list, using the state from 7.2. Verify: selecting a channel filter narrows the list to that channel only; combining a status and discount-type filter combines correctly (AND across filter types, per `specs/calendar-view/spec.md`'s new "Combining filters" scenario); changing sort re-orders the list; none of this changes the month grid's lines or the legend.

## 8. Re-verification after follow-ups

- [x] 8.1 Run `pnpm --filter mobile run build` and confirm it passes with zero type errors.
- [x] 8.2 In the web dev preview, re-exercise `specs/calendar-view/spec.md`'s "Selected-day list filter and sort" scenarios end to end, plus a visual pass confirming the line spacing/edge-to-edge fusion, bottom-anchored day number, and sage-filled today cell (with ring, when also selected) all match the maker's latest requests.

## 9. Follow-up: square corners, no-shift selection, lighter tones, thicker lines

- [x] 9.1 In `CalendarPage.vue`, change `.cal-days .day`'s `border-radius` to `0`. Add `box-sizing: border-box` and an always-present `border: 1.5px solid transparent`; change `.cal-days .day.sel` to only set `border-color: var(--ck-muted)` (no `border` shorthand, so the width/style stay inherited from the base rule). Verify by inspecting computed styles (or the rendered box) that a day's `.chan-lines` position is pixel-identical whether or not `.sel` is applied — selecting a day must not visibly shift the lines.
- [x] 9.1a **Follow-up fix, supersedes 9.1's approach:** the reserved-border fix in 9.1 broke edge-to-edge line fusion (Decision 3b) on *every* cell, not just the selected one, since a transparent border still insets the padding edge that `.chan-lines`' `left:0`/`right:0` is positioned against — caught by the maker in a follow-up screenshot showing gaps between every day's lines. Replace the border-based approach with an `outline` (`outline: 1.5px solid transparent; outline-offset: -1.5px` on the base rule, `.sel` sets only `outline-color: var(--ck-muted)`); remove `box-sizing: border-box` and the `border` rule (no longer needed — outline never affects the padding edge in the first place, so it can't cause either problem). Verify both properties together: (a) selecting a day still doesn't shift `.chan-lines` (re-check 9.1's assertion), and (b) adjacent cells' same-colored lines are visually flush with no gap, on every cell, not just non-selected ones — check a selected cell adjacent to a non-selected one specifically.
- [x] 9.2 Change `.cal-days .day.today`'s `background`/`color` from `var(--ck-sage)`/`#fff` to `var(--ck-sage-dim)`/`var(--ck-sage)` (design.md Decision 8). Verify today's cell still reads clearly as "today" against the paper background, and that the (now muted) selected ring is still visible when today is also selected.
- [x] 9.3 Change `.chan-line`'s `height` from `2px` to `3px`. Verify lines are still visually distinct from the day-cell border and from each other with the existing 4px gap.
- [x] 9.4 Run `pnpm --filter mobile run build` and re-verify visually in the web dev preview: square (non-rounded) day cells, a lighter muted selected ring, a paler today fill, thicker lines, and — most importantly — clicking between days shows no jump/shift in the line stack's position.

## 10. Follow-up: header size and week-stable line positions

- [x] 10.1 In `CalendarPage.vue`, change `.brand2`'s `font-size` from `20px` to `22px`, matching `CodesPage`'s/`SettingsPage`'s `.brand`. Verify visually side-by-side with the other two tabs' headers.
- [x] 10.2 Rewrite `getDayChannelLines` (`utils/calendar.ts`) per design.md Decision 3c: group `days` into 7-day week chunks, compute each week's active-channel set (union across all 7 days) ordered by `sortOrder`, then per day return one slot per channel in that week's order — the channel's color if active that specific day, `null` otherwise (days with zero active channels still get no map entry at all). Update the return type to `Map<string, (string | null)[]>` and `CalendarPage`'s `linesFor` to match; `null` entries bind `background: null` via Vue's style object (removing the property, i.e. an invisible same-size line) rather than being filtered out. Verify against a fixture spanning one week: a channel active only on days 1-3 of the week keeps its slot position (blank, not compacted) on days 4-7 where a different, later-starting channel is active, per `specs/calendar-view/spec.md`'s two new scenarios ("ends partway through a week" and "returns later in the same week").
- [x] 10.3 Run `pnpm --filter mobile run build` and re-verify visually in the web dev preview: the Calendar title matches Codes'/Settings' size, and — using the seeded/created coupon data — a channel's line stays at a fixed height across a week even as neighboring channels' coupons start/end mid-week, with no jump when clicking through days.
